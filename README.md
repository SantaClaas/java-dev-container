# Java dev container

A dev container template/base repository for univerisity java projects to keep them isolated from my machine.

# Run
```bash
mvn spring-boot:run
```

Vite dev server needs to accept incoming requests to the dev server
```bash
pnpm dev --host
```